{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "serve -s dist",
    "clean": "rm -rf dist && mv node_modules/.vite node_modules/.vite_backup || true",
    "railway-start": "npm run clean && npm install && npm run build && npm start"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.6.2",
    "@tanstack/react-query": "^4.30.0",
    "lucide-react": "^0.258.0",
    "date-fns": "^2.31.0"
  },
  "devDependencies": {
    "vite": "^4.5.14",
    "@vitejs/plugin-react": "^3.1.0",
    "serve": "^14.2.0"
  }
}
