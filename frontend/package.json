{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "serve -s dist",
    "clean:build": "rimraf node_modules/.vite dist && npm install && npm run build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@tanstack/react-query": "^5.3.0",
    "recharts": "^2.7.2",
    "date-fns": "^2.30.0",
    "lucide-react": "^0.268.0",
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.1.0",
    "vite": "^4.5.14",
    "rimraf": "^5.0.0"
  }
}
